---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    run: agendamentoporto
  name: agendamentoporto
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      run: agendamentoporto
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        run: agendamentoporto
    spec:
      containers:
      - image: micro-agendamento:v1
        imagePullPolicy: IfNotPresent
        name: agendamentoporto
        env:
          - name: BD_URL
            value: mongodb+srv://testemongodb:modbMAdTU3sjEqns@testandomongodb-pklgf.mongodb.net/test?retryWrites=true&w=majority
        ports:
        - containerPort: 30123
          protocol: TCP
        resources: {}
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  labels:
    run: agendamentoporto
  name: agendamentoporto
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 30123
  selector:
    run: agendamentoporto
  sessionAffinity: None
  type: ClusterIP