---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    run: realizarporto
  name: realizarporto
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      run: realizarporto
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        run: realizarporto
    spec:
      containers:
      - image: micro-realizar:v1
        imagePullPolicy: IfNotPresent
        name: realizarporto
        ports:
        - containerPort: 30123
          protocol: TCP
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      terminationGracePeriodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  labels:
    run: realizarporto
  name: realizarporto
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 30123
  selector:
    run: realizarporto
  sessionAffinity: None
  type: ClusterIP